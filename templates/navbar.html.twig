<nav class="bg-primary w-full z-20 top-0 left-0">
    <div class=" flex flex-wrap items-center justify-between mx-auto p-4 ">
        <a href="{{ path('home') }}" class="w-16 md:w-56">
            <img src="https://cdn.gruau.com/image/logo/icon-reversed.svg" class="mr-3 md:hidden" alt="Logo Gruau">
            <img src="https://cdn.gruau.com/image/logo/grp-reversed.svg" class="mr-3 md:block" alt="Logo Gruau">
        </a>
        <div class="items-center justify-between md:flex md:w-auto">
            <h1 class="text-xl md:text-4xl text-center text-white font-bold">{% if titrePage is defined %}{{ titrePage }}{% else %}Saisie des heures{% endif %}</h1>
        </div>
        <div class="flex md:order-2 w-16 md:w-56 justify-end items-center max-sm:mt-1">
            {% if app.user is not null %}
                <div class="dropdown dropdown-end" >
                    <div  tabindex="0" role="button" class="btn btn-circle m-1">
                        <span class="sr-only">Menu</span>
                        <span class="text-xl font font-bold text-center" id="couleurLettre">{{ app.user.nomEmploye|slice(0, 1) }}</span>
                    </div>
                    <div tabindex="0" class="dropdown-content z-20 menu p-2 shadow bg-primary rounded-box w-60">
                        <div class="px-4 py-3">
                            <span class="block text-sm text-white">{{ app.user.nomEmploye }}</span>
                            <span class="block text-sm text-gray-300 truncate">{{ app.user.id }}</span>
                        </div>
                        <ul>
                            <li>
                                <a href="{{ path('temps') }}" target="_self" class="block text-sm text-white focus:text-white hover:bg-secondary" onclick="this.parentNode.style.display = 'none'"><i class="fa-solid fa-pen-to-square"></i>  Saisie des temps</a>
                            </li>
                            <li>
                                <a href="{{ path('historique') }}" target="_self" class="block text-sm text-white focus:text-white hover:bg-secondary" onclick="this.parentNode.style.display = 'none'"><i class="fa-solid fa-table-columns"></i>  Détail des temps</a>
                            </li>
                            {% if is_granted('ROLE_MANAGER') %}
                                <li>
                                    <a href="{{ path('console') }}" target="_self" class="block text-sm text-white focus:text-white hover:bg-secondary" onclick="this.parentNode.style.display = 'none'"><i class="fa-solid fa-eye"></i>  Console d'approbation</a>
                                </li>
                            {% endif %}
                            <li>
                                <a id="deconnexion" href="{{ path('deconnexion') }}" target="_self" class="block text-sm text-white hover:bg-secondary" onclick="this.parentNode.style.display = 'none'"><i class="fa-solid fa-right-to-bracket text-accent"></i>  Quitter</a>
                            </li>
                        </ul>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
